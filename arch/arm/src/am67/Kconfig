#
# For a description of the syntax of this configuration file,
# see the file kconfig-language.txt in the NuttX tools repository.
#

comment "AM67 Configuration Options"

config AM67_GPIO
	bool "AM67 padconfig GPIO driver"
	default y
	---help---
		Enable the AM67 GPIO driver that drives pads configured as
		GPIO through the padconfig registers (LED, IMU_EN and the
		SPI chip selects).

config AM67_MCSPI0
	bool "AM67 MCU_MCSPI0 master driver"
	default n
	depends on SPI
	---help---
		Enable the polled SPI master driver for MCU_MCSPI0 at
		0x04b00000, used by the onboard ICM-20948 IMU (CS3) and
		LPS22DF barometer (CS1).

if AM67_MCSPI0

config AM67_MCSPI0_FCLK
	int "MCU_MCSPI0 functional clock (Hz)"
	default 48000000

endif

config AM67_I2C
	bool "AM67 I2C support"
	default n
	depends on I2C

if AM67_I2C

config AM67_I2C0
	bool "AM67 MCU_I2C0 driver (MCU domain, 0x04900000)"
	default n
	---help---
		Enable the I2C master driver for MCU_I2C0.

config AM67_WKUP_I2C0
	bool "AM67 WKUP_I2C0 driver (WKUP domain, 0x2b200000)"
	default n
	---help---
		Enable the I2C master driver for WKUP_I2C0.

endif # AM67_I2C
